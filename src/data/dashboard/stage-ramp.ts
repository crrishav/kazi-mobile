// Stage bar color ramp — a one-off light-mint → ink progression used only by
// the dashboard's "orders by stage" bar, so it isn't promoted into the shared
// theme (same reasoning as Production's own stage ramp).
export const stageRampLight = ['#CDEDDD', '#A5E0C4', '#5FD2A0', '#2FA97C', '#147A57', '#0D1F19'];
export const stageRampDark = ['#1D3129', '#25453A', '#2FA97C', '#3FC190', '#57D19E', '#6FDDA9'];
