import { Marketing } from '@/screens/marketing/marketing';

/** Marketing as a pushed page — see `module/production.tsx`. */
export default function MarketingModuleRoute() {
  return <Marketing />;
}
