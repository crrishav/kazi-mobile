import { Inventory } from '@/screens/inventory/inventory';

/** Inventory as a pushed page — see `module/production.tsx`. */
export default function InventoryModuleRoute() {
  return <Inventory />;
}
